language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t zihan42/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ZihanZhangdocker-react npm run test